var maxDrops=100

for(var i=0; i<maxdrops; i++){

    drops.push(new createDrop(random)(0,400),random(0,400)); 
} 
frition:0.1 

if(this.rain.posoition.y>height){

    Matter.Body.setPosoition(this.rain,{x:random(0,400),y:random(0,400)})
}